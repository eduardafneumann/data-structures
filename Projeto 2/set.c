#include "avl.h"

SET *set_interseccao(AVL *arvore1, AVL *arvore2){
    ITEM **array=avl_get_array(arvore);
    while(array[i]!=NULL){
        if(arvore2)
        i++;
    }
}

SET *set_uniao(AVL *arvore1, AVL*arvore2){
    ITEM *array1= avl_get_array(arvore1); 
    ITEM *array2= avl_get_array(arvore2); 
} 